export interface Istates {
  activeSuggestion?: number;
  filteredSuggestions?: [];
  showSuggestions?: boolean;
  userInput?: string;
}

export interface IBookSearchStates {
  selectedBook?: object;
}
